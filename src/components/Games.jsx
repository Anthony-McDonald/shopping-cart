const Games = () => {
    // const {name} = useParams();
    return (
        <div>
            <h1>Hello from profile page!</h1>
            <p>So, how are you?</p>
        </div>
    )
}

export default Games;
